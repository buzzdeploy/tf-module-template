on:
  workflow_call:
    inputs:
      run-name:
        required: false
        description: 'The name shown when executed'
        default: 'Running SAST 🚀'
        type: string

name: SAST
jobs:
  sast:
    runs-on: ubuntu-latest
    steps:
    - name: taco 
      run: echo "Scanning..."
